export const jwtConstants = {
  secret: 'IndriverTracking_SecretKey', // Secret key for JWT signing and verification. In a real application, this should be stored in an environment variable and not hardcoded.
};